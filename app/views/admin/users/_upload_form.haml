= form_for([:admin, @user, @user.receipts.build], :html => {:multipart => true, :id => "file_upload", :method => "post"}) do |f|
  = f.file_field :img
  %button Upload
  #div Upload Receipts

=render :partial => "receipts/upload_result"
