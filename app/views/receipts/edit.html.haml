.block
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Receipt
    .inner
      = form_for @receipt, :url => receipt_path(@receipt), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
