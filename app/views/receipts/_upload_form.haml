= form_for([current_user, current_user.receipts.build], :format => :json, :html => {:multipart => true, :id => "file_upload", :method => "post"}) do |f|
  = f.file_field :img
  %button Upload
  #div Upload Receipts

=render :partial => "receipts/upload_result"
